import { MtAlertDirective } from './mt-alert.directive';

describe('MtAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new MtAlertDirective();
    expect(directive).toBeTruthy();
  });
});
